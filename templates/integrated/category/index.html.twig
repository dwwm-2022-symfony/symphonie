{% extends 'base.html.twig' %}

{% block title %}Les Catégories{% endblock %}

{% block main %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="row">
                    <div class="col-12 py-3 d-flex justify-content-between align-items-center">
                        <p>Les catégories</p>
                        <a href="#" class="text-decoration-underline"><i class="fa-solid fa-plus"></i> Ajouter une catégories</a>
                    </div>
                    {% for i in 0..30 %}
                        <div class="col-12 col-md-6 col-lg-4 mb-3">
                            <a href="#" class="category-card bordered">
                                Nom catégorie
                            </a>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
